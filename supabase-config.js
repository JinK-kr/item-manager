/* =========================================================
   supabase-config.js — 내 Supabase 프로젝트 주소와 키
   =========================================================

   값 찾는 곳:
     Supabase 대시보드 → 프로젝트 선택 → 왼쪽 아래 Project Settings
     → API → 아래 두 값을 복사한다

       Project URL   →  url
       anon public   →  anonKey     (여기에 넣을 것)

   ⚠ service_role 키는 절대 여기에 넣지 마세요.
      그 키는 모든 정책을 무시하고 DB 전체를 조작할 수 있습니다.
      이 파일은 브라우저로 전송되고 깃허브에도 올라갑니다.

   anon 키는 원래 브라우저에 공개되는 값이라 여기 적어도 됩니다.
   대신 표를 지키는 건 오직 RLS 정책뿐입니다.
   지금 정책은 '누구나 읽고 쓰기'로 열려 있습니다 (supabase/schema.sql 참고).
   ========================================================= */

window.SUPABASE_CONFIG = {
  url:     'https://여기에-프로젝트-URL을-넣으세요.supabase.co',
  anonKey: '여기에-anon-public-키를-넣으세요'
};
